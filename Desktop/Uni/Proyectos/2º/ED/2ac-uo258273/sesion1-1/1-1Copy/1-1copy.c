//


int copy(char * source, char * destination, unsigned int lengthDestination)
{
    char source= &source;
    if(source.sizeof<=lengthDestination)
    {
        char aux =&source
        &destination=aux;
        return 0;
    }
    return -1;
}
